c_stdlib:
  - if: linux
    then: sysroot
  - if: osx
    then: macosx_deployment_target
  - if: win
    then: vs
c_stdlib_version:
  - if: linux
    then: 2.28
  - if: osx
    then: 14.2
c_compiler:
  - if: win
    then: vs2026
cxx_compiler:
  - if: win
    then: vs2026
